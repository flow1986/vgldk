
NAME = dungeonbash

# For targeting a specific model:
#VGLDK_SERIES = 1000
#VGLDK_SERIES = 2000
#VGLDK_SERIES = 4000
VGLDK_SERIES = 6000

# More monsters + inventory/magic no longer fit an 8KB cart (AT28C64B);
# using a 32KB one (AT28C256) instead - see /memories or the session notes.
CART_SIZE_KB = 32

all: cart
include ../../tools/Makefile.mk
